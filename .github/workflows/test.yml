name: CI
on:
  push:
    branches:
      - main
    
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run:  pip install -r requirements.txt

    - name: Install Robot Framework
      run:  rfbrowser init

    - name: Run Robot Framework
      run:  robot -d result/pass Testsuites/test_pass.robot

    - name: Download File
        run: curl -o log.txt https://github.com/WorrapongT/demo/home/runner/work/demo/demo/result/pass/log.html